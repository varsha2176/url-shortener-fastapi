[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    asyncio: mark test as async
    slow: mark test as slow
timeout = 30
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings